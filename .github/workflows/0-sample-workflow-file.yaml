name: Sample Workflow

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message
        run: echo "First Workflow Job"
      - name: multiple commands
        run: |
          hostname
          ls -l /
          cat /etc/hosts
          uptime